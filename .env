# Variabili per docker-compose (configura sul server Ubuntu)
# Crea un file .env nella root del progetto con questi valori:

# Password email Aruba (obbligatoria per invio preventivi)
SMTP_PASS=Sy2!W-@UbcxqavX

# Origine CORS consentita (opzionale, default: *)
# CORS_ORIGIN=https://ravai.it

# --- Frontend (sviluppo locale) ---
# URL del backend API (lascia vuoto per usare lo stesso dominio con proxy reverso)
# Per sviluppo locale con backend su porta 3000:
# VITE_API_URL=http://localhost:3000
